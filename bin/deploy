#!/usr/bin/env bash

set -e

git pull
git add .
git commit -am "auto commit"

bin/tools/composer-update
composer check
#bin/tools/composer-update
git commit -am "increase version"
git push
#git tag "$VERSION"
#git push --tags
